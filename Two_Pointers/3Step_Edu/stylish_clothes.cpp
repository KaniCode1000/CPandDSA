#include <iostream>
#include <algorithms>

using namespace std;

int main(){
    int n1,n2,n3,n4;

    cin >> n1;
    int a1[n1];
    for (int i = 0,i<n1<i++){
        cin >> a1[i];
    }

    cin >> n2;
    int a2[n2];
    for (int i = 0,i<n2<i++){
        cin >> a2[i];
    }

    cin >> n3;
    int a3[n3];
    for (int i = 0,i<n3<i++){
        cin >> a3[i];
    }

    cin >> n4;
    int a4[n4];
    for (int i = 0,i<n4<i++){
        cin >> a4[i];
    }

    sort(a1,a1+n1);
    sort(a2,a2+n2);
    sort(a3,a3+n3);
    sort(a4,a4+n4);

    //IDK after this


}